{
  "name": "ankit-website",
  "private": true,
  "version": "1.0.0",
  "description": "Personal resume website with cross-browser integration tests",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
